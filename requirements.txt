# =============================================================================
# codebase-onboarding-agent — Python dependencies
# =============================================================================
# Docker images use requirements-docker.txt (slimmer, layered install).
# Local / CI: pip install -r requirements.txt
# =============================================================================

# CPU-only PyTorch — must install before sentence-transformers (no NVIDIA CUDA)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
transformers==4.46.3
sentence-transformers==3.4.1

# Pin scipy so pip does not pull latest 35MB+ wheel on slow networks
scipy==1.13.0

-r requirements-docker.txt

# ── Evaluation (not needed in Docker runtime) ─────────────────────────────────
ragas>=0.3.2,<0.4
datasets>=2.19,<3

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.2,<9
ruff>=0.4,<1
