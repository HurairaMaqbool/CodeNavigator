[pytest]
testpaths = tests
norecursedirs = data repos .venv bm25_index chroma_db graph_store .git .github scratch scripts admin docs eval
python_files = test_*.py
addopts = -v
markers =
    live_api: marks tests as requiring live LLM API access (deselect with '-m "not live_api"')
