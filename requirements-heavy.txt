# Large wheels installed in separate Dockerfile layers before requirements-docker.txt.
# scipy has its own layer in the Dockerfile (slow-network retry).
numpy==2.1.3
scipy==1.13.0
scikit-learn==1.5.2
pyarrow==17.0.0
pandas==2.2.3
